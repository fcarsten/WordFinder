<?xml version="1.0" encoding="UTF-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    android:versionCode="12" android:versionName="3.01"
    android:installLocation="auto"
	xmlns:android="http://schemas.android.com/apk/res/android">
	<uses-permission android:name="android.permission.VIBRATE" />
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

	<application android:icon="@mipmap/ic_launcher"
		android:roundIcon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:theme="@style/Theme.AppCompat" android:allowBackup="true"
        tools:ignore="GoogleAppIndexingWarning">
		<activity android:name="org.carstenf.wordfinder.WordFinder" android:exported="true">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>
		<activity android:name="org.carstenf.wordfinder.WordFinderPreferences" android:label="@string/set_preferences">
		</activity>
	</application>
</manifest>
