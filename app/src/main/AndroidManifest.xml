<?xml version="1.0" encoding="UTF-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    android:versionCode="7" android:versionName="2.7"
    android:installLocation="auto"
	xmlns:android="http://schemas.android.com/apk/res/android">
	<uses-permission android:name="android.permission.VIBRATE" />

	<application android:icon="@mipmap/ic_launcher"
		android:roundIcon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:theme="@style/Theme.AppCompat" android:allowBackup="true"
		android:fullBackupContent="true"
        tools:ignore="GoogleAppIndexingWarning">
		<activity android:name="org.carsten.WordFinder" android:exported="true">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>
		<activity android:name="org.carsten.WordFinderPreferences" android:label="@string/set_preferences">
		</activity>
	</application>
</manifest>
